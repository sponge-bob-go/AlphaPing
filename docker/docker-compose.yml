version: '3.9'

services:
  app:
    build: 
      context: ../app
      dockerfile: ../docker/app/Dockerfile
    container_name: go-app
    ports:
      -"8080:8080"
    depends_on:
      - db
    environment:
      - DB_HOST:db
      - DB_PORT:5432
  db:
    image: postgres:16